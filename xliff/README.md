# Translated XLIFF workspace

Place Danish XLIFF 1.2 files in this directory, then run:

```bash
pnpm import:xlf -- xliff
```

The source catalog can be generated with `pnpm source:export`. XLIFF files are intentionally kept
in the repository so translation changes can be reviewed independently from generated JSON.
